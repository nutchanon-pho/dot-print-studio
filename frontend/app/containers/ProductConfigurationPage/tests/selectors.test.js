// import { fromJS } from 'immutable';
// import { selectProductConfigurationPageDomain } from '../selectors';

describe('selectProductConfigurationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
